{
  "name": "cdcitas",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:migrate:local": "wrangler d1 migrations apply control-de-citas-db --local",
    "db:migrate:remote": "wrangler d1 migrations apply control-de-citas-db --remote"
  },
  "devDependencies": {
    "wrangler": "^4.131.2"
  }
}
